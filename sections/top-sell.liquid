<div class="pt-16">
  <div class="container">
    <h2 class="rff-section-title text-center">Top selling</h2>
    <div class="flex justify-between">
      {% for i in (1..4) %}
        <div class="w-1/4 px-4">
          {% render 'single-product' %}
        </div>
      {% endfor %}
    </div>
    <div class="mt-10 text-center">
      <a href="" class="rounded-full px-12 py-2 border border-gray-200 inline-block">View all</a>
    </div>
  </div>
</div>
